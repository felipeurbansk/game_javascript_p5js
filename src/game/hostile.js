class Hostile extends Animate {
  constructor(
    mapeamento,
    image,
    position_x,
    widthChar,
    heightChar,
    widthSprite,
    heightSprite
  ) {
    super(
      mapeamento,
      image,
      position_x,
      widthChar,
      heightChar,
      widthSprite,
      heightSprite
    );
  }
}
